# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# Gemini API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Exa API key from https://dashboard.exa.ai — used for live rent comps when set.
# Takes priority over HERMES_URL and the local Playwright agent.
EXA_API_KEY=your-exa-api-key-here

# Optional Exa tuning (defaults shown):
# EXA_EFFORT=medium
# EXA_TIMEOUT_MS=180000

# Optional: hermes desktop agent URL (ngrok/Cloudflare tunnel exposing port 8001).
# Used only when EXA_API_KEY is not set.
# Example: HERMES_URL=https://xxxx-xx-xx-xx.ngrok-free.app
HERMES_URL=
