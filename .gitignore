# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Design system source zip (frontend/ is the unpacked version)
MyPropertyIQ Design System.zip

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Deploy bundle scratch
fyp2-deploy.tgz

# Random Forest valuation artifact (~203 MB) — exceeds GitHub's 100 MB file
# limit and is a reproducible build output of `python backend/save_models.py rf`.
# Reproducible build output — not tracked in git.
backend/artifacts/valuation_rf.joblib

# Office lock files
~$*

# Location hierarchy edge list — analysis byproduct of build_location_hierarchy.py;
# only location_hierarchy.json is loaded at runtime
processed data/location_hierarchy_edges.csv

# Environment and cache files
backend/.env
# backend/.cache/ is committed so pulled clones share pre-fetched Exa results
